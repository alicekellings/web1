# VideoTools Hub - 项目说明

## 📋 项目概述

这是一个专注于视频文件处理的在线工具网站，主要针对 "open avi file"、"avi to mp4" 等高搜索量关键词。

### 核心特点
- ✅ 纯静态网站（HTML/CSS/JS）
- ✅ 本地处理文件（隐私安全）
- ✅ 零成本部署（Vercel/Netlify）
- ✅ SEO 优化（每个工具独立页面）
- ✅ 响应式设计（支持所有设备）

---

## 🎯 目标关键词

### 已实现的页面
1. **Open AVI File Online** (10,000+ 月搜索)
   - 路径: `/open-avi-file-online/`
   - 功能: 打开和播放 AVI 文件

### 待开发的页面（按优先级）
2. **AVI to MP4 Converter** (50,000+ 月搜索)
3. **Play AVI Online** (8,000+ 月搜索)
4. **Convert AVI to MP4 Online** (30,000+ 月搜索)
5. **Video Format Converter** (25,000+ 月搜索)
6. **Compress AVI File** (5,000+ 月搜索)

---

## 🏗️ 项目结构

```
web1/
├── index.html                    # 首页（工具导航）
├── package.json                  # 项目配置
├── vercel.json                   # Vercel 部署配置
├── README.md                     # 英文说明
├── 项目说明.md                   # 中文说明（本文件）
├── start-server.bat              # 本地启动脚本
│
├── styles/                       # 全局样式
│   └── main.css
│
├── scripts/                      # 全局脚本
│   └── main.js
│
└── open-avi-file-online/        # 工具页面示例
    ├── index.html               # 页面HTML
    ├── styles.css               # 页面样式
    └── app.js                   # 页面功能
```

---

## 🚀 快速开始

### 1. 本地运行

**方法1：使用批处理文件（Windows）**
```bash
双击 start-server.bat
```

**方法2：使用 Python**
```bash
python -m http.server 8000
```

**方法3：使用 Node.js**
```bash
npx http-server -p 8000
```

然后访问: `http://localhost:8000`

### 2. 部署到 Vercel

```bash
# 1. 安装 Vercel CLI
npm i -g vercel

# 2. 登录
vercel login

# 3. 部署
vercel

# 4. 生产环境部署
vercel --prod
```

### 3. 部署到 GitHub + Vercel

```bash
# 1. 初始化 Git
git init
git add .
git commit -m "Initial commit"

# 2. 推送到 GitHub
git remote add origin https://github.com/你的用户名/videotools-hub.git
git push -u origin main

# 3. 在 Vercel 导入项目
# 访问 vercel.com，点击 "Import Project"，选择你的 GitHub 仓库
```

---

## 📝 如何添加新工具

### 步骤1：创建工具目录

```bash
mkdir your-tool-name
cd your-tool-name
```

### 步骤2：复制模板文件

从 `open-avi-file-online/` 复制以下文件：
- `index.html`
- `styles.css`
- `app.js`

### 步骤3：修改内容

**修改 `index.html`:**
1. 更新 `<title>` 和 meta 标签
2. 更新页面标题和描述
3. 更新 SEO 内容部分

**修改 `styles.css`:**
- 根据需要调整样式（通常不需要大改）

**修改 `app.js`:**
- 实现工具的核心功能

### 步骤4：添加到首页

在 `index.html` 中添加工具卡片：

```html
<div class="tool-card" data-category="converter">
    <a href="/your-tool-name/">
        <div class="tool-header">
            <span class="tool-icon">🔄</span>
            <span class="tool-badge new">New</span>
        </div>
        <h3>Your Tool Name</h3>
        <p>Tool description here...</p>
        <div class="tool-tags">
            <span class="tag">Tag1</span>
            <span class="tag">Tag2</span>
        </div>
    </a>
</div>
```

---

## 🎨 设计系统

### 颜色方案

```css
--primary: #6366f1;      /* 主色（紫蓝色） */
--primary-dark: #4f46e5; /* 主色深色 */
--bg-main: #0f172a;      /* 主背景（深色） */
--bg-card: #1e293b;      /* 卡片背景 */
--text-primary: #f1f5f9; /* 主文字 */
--text-secondary: #94a3b8; /* 次要文字 */
--accent: #22d3ee;       /* 强调色（青色） */
--success: #10b981;      /* 成功（绿色） */
--danger: #ef4444;       /* 危险（红色） */
```

### 组件样式

所有工具页面使用统一的组件：
- 导航栏 (`.navbar`)
- 上传区域 (`.upload-area`)
- 按钮 (`.btn`)
- 信息面板 (`.info-panel`)
- 状态消息 (`.status-message`)

---

## 🔧 技术实现

### 当前功能

1. **文件上传**
   - 拖拽上传
   - 点击选择
   - 文件验证

2. **视频播放**
   - HTML5 video 标签
   - 本地文件播放
   - 全屏支持

3. **文件信息**
   - 文件名、大小
   - 视频时长、分辨率
   - 自动提取元数据

### 未来功能（需要 FFmpeg.wasm）

1. **视频转换**
   - AVI → MP4
   - MP4 → AVI
   - 其他格式转换

2. **视频压缩**
   - 调整质量
   - 减小文件大小

3. **音频提取**
   - 提取 MP3
   - 提取 WAV

### 集成 FFmpeg.wasm

```html
<!-- 在 HTML 中引入 -->
<script src="https://cdn.jsdelivr.net/npm/@ffmpeg/ffmpeg@0.12.6/dist/umd/ffmpeg.js"></script>
```

```javascript
// 在 JS 中使用
const { FFmpeg } = FFmpegWASM;
const ffmpeg = new FFmpeg();

await ffmpeg.load();
await ffmpeg.writeFile('input.avi', await fetchFile(file));
await ffmpeg.exec(['-i', 'input.avi', 'output.mp4']);
const data = await ffmpeg.readFile('output.mp4');
```

---

## 📊 SEO 优化

### 每个页面必须包含

1. **Meta 标签**
   ```html
   <title>关键词 - 描述</title>
   <meta name="description" content="...">
   <meta name="keywords" content="...">
   <link rel="canonical" href="...">
   ```

2. **Open Graph**
   ```html
   <meta property="og:title" content="...">
   <meta property="og:description" content="...">
   <meta property="og:url" content="...">
   ```

3. **结构化数据**
   ```html
   <script type="application/ld+json">
   {
     "@context": "https://schema.org",
     "@type": "WebApplication",
     ...
   }
   </script>
   ```

4. **内容优化**
   - H1 标题包含核心关键词
   - H2 标题包含长尾关键词
   - FAQ 部分回答常见问题
   - 相关工具链接

---

## 💰 变现策略

### 1. 免费版（当前）
- 所有基础功能免费
- 文件大小限制：500MB
- 本地处理

### 2. 广告收入（未来）
- Google AdSense
- 侧边栏广告
- 内容中广告

### 3. 付费版（未来）
- 更大文件支持（2GB）
- 批量处理
- 更快转换速度
- 无广告
- 价格：$9.99/月

### 4. 联盟营销
- 推荐视频编辑软件
- 推荐云存储服务
- 推荐相关工具

---

## 📈 推广计划

### 第1周：完成 MVP
- ✅ 首页
- ✅ Open AVI File Online 页面
- ⏳ 本地测试

### 第2周：部署和推广
- 部署到 Vercel
- Product Hunt 发布
- Reddit 推广（r/InternetIsBeautiful）
- Indie Hackers 分享

### 第3周：添加更多工具
- AVI to MP4 Converter
- Play AVI Online
- Video Format Converter

### 第4周：SEO 优化
- 提交到 Google Search Console
- 创建 sitemap.xml
- 优化页面加载速度
- 添加博客内容

---

## 🐛 常见问题

### Q: 为什么视频不能播放？
A: 确保浏览器支持该视频格式。AVI 文件可能需要特定的编解码器。

### Q: 如何添加视频转换功能？
A: 需要集成 FFmpeg.wasm 库。参考上面的"集成 FFmpeg.wasm"部分。

### Q: 文件大小限制是多少？
A: 当前限制是 500MB。可以在代码中调整 `maxSize` 变量。

### Q: 支持哪些浏览器？
A: 支持所有现代浏览器（Chrome, Firefox, Safari, Edge）。

---

## 📞 联系方式

如有问题或建议，请联系：
- Email: your-email@example.com
- GitHub: https://github.com/yourusername

---

## ✅ 下一步

1. **测试当前功能**
   ```bash
   # 启动本地服务器
   python -m http.server 8000
   
   # 访问 http://localhost:8000
   # 测试上传和播放 AVI 文件
   ```

2. **部署到 Vercel**
   ```bash
   vercel
   ```

3. **开始推广**
   - Product Hunt
   - Reddit
   - Indie Hackers

4. **添加更多工具**
   - 复制 `open-avi-file-online` 目录
   - 修改内容
   - 添加到首页

---

**祝你成功！🚀**
